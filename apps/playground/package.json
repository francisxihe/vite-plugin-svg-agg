{
  "name": "playground-basic",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build ",
    "test:gzip": "npm run build && http-server dist --cors --gzip -c-1",
    "test:br": "npm run build && http-server dist --cors --brotli -c-1"
  },
  "dependencies": {
    "etag": "^1.8.1",
    "fast-glob": "^3.2.11",
    "fs-extra": "^10.0.0",
    "svgo": "^2.8.0",
    "vue": "^3.2.29"
  },
  "devDependencies": {
    "vite-plugin-svg-agg": "workspace:*"
  }
}
